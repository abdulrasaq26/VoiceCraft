# The notebook installs these itself; this file is for running the package
# locally (the contract tests need only pydantic).
#
# vLLM is pinned because it is the one dependency where "latest" is a real
# risk: the structured-output API has been renamed twice, and installing a
# newer transformers alongside it will replace the version it was built
# against and break the server.
vllm==0.27.1
fastapi
uvicorn[standard]
pydantic>=2.0
openai
pyngrok
