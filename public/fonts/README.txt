Drop a heavy display font here as thumbnail.woff2 (or .woff/.otf/.ttf).

It is picked up automatically by the thumbnail and graphic renderers — no
config, no rebuild. Without one, everything falls back to Arial Black.

Good free choices: Anton, Archivo Black, Bebas Neue, Montserrat ExtraBold.
