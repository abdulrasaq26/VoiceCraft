# Blvck-TTS is Puter-first — every AI capability (TTS, chat, images, video)
# runs through Puter's client-side SDK, so no API keys are stored on the
# server. The only thing you can tune here is the HTTP port.

# PORT=3000
